const $=id=>document.getElementById(id),asset=new URLSearchParams(location.search).get('asset');let manifest,part=0,revision=0,busy=true,font=20,pdf,saveQueue=Promise.resolve(),conflict=false,loaded=false,renderTask;
async function api(path,method='GET',body){const r=await fetch(path,{method,headers:{'Content-Type':'application/json','X-Archivist-Action':'1'},body:body?JSON.stringify(body):undefined});const data=await r.json();if(!r.ok)throw Error(data.error||'Unable to open book');return data}
function status(s){$('reader-status').textContent=s}
function fraction(){const h=document.documentElement.scrollHeight-innerHeight;return h>0?Math.max(0,Math.min(1,scrollY/h)):0}
function save(){if(busy||!loaded||conflict)return saveQueue;const p={part,fraction:fraction()};saveQueue=saveQueue.then(async()=>{if(conflict)return;try{const result=await api('/api/assets/'+asset+'/reading-progress','PUT',{...p,revision});revision=result.revision}catch(e){conflict=true;status('Position not synced: '+e.message)}});return saveQueue}
async function show(index,offset=0){busy=true;part=index;$('reading').replaceChildren();$('sections').value=String(index);$('position').textContent=(index+1)+' / '+manifest.parts.length;$('previous').disabled=index===0;$('next').disabled=index>=manifest.parts.length-1;try{if(manifest.format==='Ebook'){const data=await api('/api/assets/'+asset+'/reader/'+index);for(const text of data.paragraphs){const p=document.createElement('p');p.textContent=text;$('reading').append(p)}}else if(manifest.format==='Comic'){const img=document.createElement('img');img.alt='Page '+(index+1);img.src='/api/assets/'+asset+'/reader/'+index;$('reading').append(img);await img.decode()}else{const page=await pdf.getPage(index+1),scale=Math.min(2,Math.max(.5,$('reading').clientWidth/page.getViewport({scale:1}).width));const viewport=page.getViewport({scale}),canvas=document.createElement('canvas');canvas.width=viewport.width;canvas.height=viewport.height;canvas.setAttribute('aria-label','PDF page '+(index+1));$('reading').append(canvas);renderTask=page.render({canvasContext:canvas.getContext('2d'),viewport});await renderTask.promise}loaded=true;requestAnimationFrame(()=>{scrollTo(0,offset*Math.max(0,document.documentElement.scrollHeight-innerHeight));busy=false});status('')}catch(e){busy=false;loaded=false;status(e.message)}}
async function move(n){if(busy)return;await save();await show(Math.max(0,Math.min(manifest.parts.length-1,part+n)))}$('next').onclick=()=>move(1);$('previous').onclick=()=>move(-1);$('sections').onchange=async()=>{if(busy)return;await save();await show(Number($('sections').value))};$('appearance').value=localStorage.getItem('reader-appearance')||'light dark';function appearance(){document.documentElement.style.colorScheme=$('appearance').value;localStorage.setItem('reader-appearance',$('appearance').value)}$('appearance').onchange=appearance;appearance();function resize(n){font=Math.max(14,Math.min(36,font+n));$('reading').style.fontSize=font+'px'}$('smaller').onclick=()=>resize(-2);$('larger').onclick=()=>resize(2);document.addEventListener('visibilitychange',()=>{if(document.hidden)save()});setInterval(save,5000);
try{if(!/^\d+$/.test(asset||''))throw Error('Choose a book from Library.');status('Opening…');manifest=await api('/api/assets/'+asset+'/reader');const progress=await api('/api/assets/'+asset+'/reading-progress');revision=progress.revision;if(manifest.format==='PDF'){const lib=await import('/vendor/pdf.mjs');lib.GlobalWorkerOptions.workerSrc='/vendor/pdf.worker.mjs';pdf=await lib.getDocument({url:'/api/assets/'+asset,withCredentials:true,isEvalSupported:false,useWasm:false}).promise;manifest.parts=Array.from({length:pdf.numPages},(_,i)=>'Page '+(i+1))}manifest.parts.forEach((name,i)=>$('sections').add(new Option(name,String(i))));await show(Math.min(progress.part,manifest.parts.length-1),progress.fraction)}catch(e){status(e.message)}
